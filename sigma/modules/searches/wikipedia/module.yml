name:             Wikipedia Commands

category:         searches

enabled:          false

commands:
  - name:         wikipedia
    alts:
      - "wiki"
    enabled:      true
    usage:        "{pfx}{cmd} Thread (Computing)"
    description:
      Returns the summary of a wikipedia page that you inputted the search for.
      If a search is too general, an error will be returned.
