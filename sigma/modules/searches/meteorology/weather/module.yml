category: searches
commands:
  - alts:
      - we
    description: 'Shows the current weather for the specified location.
    You can add "unit:x" as the last argument where x is one of c, f, metric, or imperial.'
    enabled: true
    name: weather
    usage: '{pfx}{cmd} Belgrade unit:si'
enabled: true
name: Weather Commands
